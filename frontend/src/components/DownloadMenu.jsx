import React from 'react';
import ExportMenu from './ExportMenu';

/** Header downloads control — thin wrapper around shared ExportMenu. */
export default function DownloadMenu() {
  return <ExportMenu variant="icon" />;
}
